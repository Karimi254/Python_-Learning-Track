List value is a mutable data type. that is it can have values added, removed and changed.
String value is immutable. It cannot be changed once assigned
- reassign a string results to a TypeError.
-only way to mutate a string is to use slicing and concatenation to build a new by copying parts of an old string.
